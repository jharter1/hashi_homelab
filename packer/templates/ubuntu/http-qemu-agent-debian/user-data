#cloud-config
autoinstall:
  version: 1
  locale: en_US
  keyboard:
    layout: us
  
  ssh:
    install-server: true
    allow-pw: true
  
  # Install qemu-guest-agent package
  packages:
    - qemu-guest-agent
  
  storage:
    layout:
      name: direct
  
  # Enable qemu-guest-agent service on first boot
  late-commands:
    - curtin in-target --target=/target -- systemctl enable qemu-guest-agent
  
  user-data:
    disable_root: false
    users:
      - name: packer
        passwd: "$6$WIsM0AXoJG.fM91k$tKbOOMzIBjIyQ68vfmSbpbD7AYLVDkbC1ZVPdcBi3iGsuk5Puet0v4.OD8aakxAdGlU6wOYi/OdqGZPgd3XOx."
        groups: [adm, sudo]
        lock-passwd: false
        sudo: ALL=(ALL) NOPASSWD:ALL
        shell: /bin/bash
