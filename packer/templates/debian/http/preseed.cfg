# Debian preseed file for automated installation
# Locale and keyboard
d-i debian-installer/locale string en_US.UTF-8
d-i keyboard-configuration/xkb-keymap select us

# Network configuration
d-i netcfg/choose_interface select auto
d-i netcfg/get_hostname string debian
d-i netcfg/get_domain string localdomain

# Mirror settings
d-i mirror/country string manual
d-i mirror/http/hostname string deb.debian.org
d-i mirror/http/directory string /debian
d-i mirror/http/proxy string

# User account
d-i passwd/user-fullname string Packer User
d-i passwd/username string packer
# Password: packer (generated with: printf "packer" | openssl passwd -6 -stdin)
d-i passwd/user-password-crypted password $6$Pp5wTpcIdEDH7eCT$g/SQLxXVfxQAFmkueYqzACeZQNsQfU23tYQy3MOUlLUF10rMJsba3hA5pEqsgFkMMBFG4sNaU/MD4YtdwVfMO/
d-i passwd/user-default-groups string audio cdrom video sudo

# Disable root account
d-i passwd/root-login boolean false

# Clock and timezone
d-i clock-setup/utc boolean true
d-i time/zone string UTC
d-i clock-setup/ntp boolean true

# Partitioning
d-i partman-auto/method string regular
d-i partman-auto/choose_recipe select atomic
d-i partman-partitioning/confirm_write_new_label boolean true
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true

# Package selection - absolute minimum
tasksel tasksel/first multiselect standard
d-i pkgsel/include string sudo openssh-server cloud-init qemu-guest-agent
d-i pkgsel/upgrade select none
d-i pkgsel/update-policy select none
popularity-contest popularity-contest/participate boolean false

# Skip additional packages and documentation
d-i pkgsel/install-language-support boolean false

# Boot loader
d-i grub-installer/only_debian boolean true
d-i grub-installer/bootdev string default

# Finish installation
d-i finish-install/reboot_in_progress note

# Late command - enable qemu-guest-agent and configure sudo
d-i preseed/late_command string \
    in-target systemctl enable qemu-guest-agent; \
    in-target systemctl enable ssh; \
    echo 'packer ALL=(ALL) NOPASSWD:ALL' > /target/etc/sudoers.d/packer; \
    in-target chmod 440 /etc/sudoers.d/packer; \
    echo 'PermitRootLogin no' >> /target/etc/ssh/sshd_config; \
    echo 'PasswordAuthentication yes' >> /target/etc/ssh/sshd_config; \
    echo 'PubkeyAuthentication yes' >> /target/etc/ssh/sshd_config
