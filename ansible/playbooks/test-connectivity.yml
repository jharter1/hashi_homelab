---
- name: Test connectivity to all hosts
  hosts: all
  gather_facts: yes

  tasks:
    - name: Ping all hosts
      ansible.builtin.ping:

    - name: Display host information
      ansible.builtin.debug:
        msg: "{{ inventory_hostname }} ({{ ansible_host }}) - {{ node_type | default('unknown') }}"
